{
  "name": "daraja",
  "version": "0.0.1",
  "private": true,
  "description": "Monorepo for the Daraja (M-Pesa) SDKs and MCP server",
  "scripts": {
    "build:mcp": "npm --prefix mcp run build",
    "test:mcp": "npm --prefix mcp run test"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.5.0"
  },
  "workspaces": [
    "mcp"
  ]
}
